namespace Squadron
{
    /// <summary>
    /// Defines the type of the link.
    /// </summary>
    public enum ComposeLinkType
    {
        /// <summary>
        /// Access from host
        /// </summary>
        Host,
        /// <summary>
        /// Access from another container
        /// </summary>
        Container
    }
}
